# ─────────────────────────────────────────────────────────────────────────────
# secret-postgres.example.yaml  — EXAMPLE ONLY: placeholders, NOT real credentials
#
# Before deploying, create the real secret with:
#
#   kubectl create secret generic port-ops-postgres \
#     --namespace port-ops-demo \
#     --from-literal=username=portops \
#     --from-literal=password=<YOUR_POSTGRES_PASSWORD>
# ─────────────────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: port-ops-postgres
  namespace: port-ops-demo
type: Opaque
data:
  # echo -n 'REPLACE_ME' | base64  → UkVQTEFDRV9NRQ==
  username: UkVQTEFDRV9NRQ==
  password: UkVQTEFDRV9NRQ==
