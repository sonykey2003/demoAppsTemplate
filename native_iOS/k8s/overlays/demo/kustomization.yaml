apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Demo overlay. Kept thin on purpose: the base is already demo-ready. Add
# environment-specific patches here (replicas, image tags, resource tuning) without
# touching the base manifests.
resources:
  - ../../base
